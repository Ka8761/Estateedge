#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../mkdirp/bin/cmd.js" "$@"
else
  exec node  "$basedir/../../../mkdirp/bin/cmd.js" "$@"
fi
# cmd-shim-target=C:/Users/DIKACHI/Desktop/EstateEdge/node_modules/.pnpm/ts-node-dev@2.0.0_@types+node@20.19.41_typescript@5.9.3/node_modules/mkdirp/bin/cmd.js
